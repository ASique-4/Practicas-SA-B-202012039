type Restaurant {
  name: String
  menu: [String]
  hours: String
}

type Query {
  restaurantInfo: Restaurant
}

type Hour {
  day: String!
  open: String!
  close: String!
}

type MenuItem {
  id: ID!
  name: String!
  description: String
  price: Float!
}